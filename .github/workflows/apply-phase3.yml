name: Apply phase three

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-phase3.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply phase three source changes
        run: node scripts/apply-phase3.mjs
      - name: Commit generated changes
        run: |
          if git diff --quiet -- src/App.tsx src/main.tsx supabase/SETUP.sql; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx src/main.tsx supabase/SETUP.sql
          git commit -m "Add moderated editing and multi-family memberships"
          git push
